# 默认模型配置
DEFAULT_MODEL_API_KEY=your-api-key-here
DEFAULT_MODEL_ENDPOINT=https://api.openai.com/v1
DEFAULT_MODEL_NAME=gpt-4o

# 加密主密码 (首次运行时由用户设置)
MASTER_PASSWORD_HASH=

# 数据库路径
DB_PATH=./data/chat-assistant.db

# 日志级别
LOG_LEVEL=info
